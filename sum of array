#include<stdio.h>
int main(){
    int i,a[5],sum,*p;
    p=&sum;
    printf("Enter 5 value of array :");
    for(i=0;i<5;i++){
        scanf("%d",&a[i]);
        sum+=a[i];
    }
    printf("The value of array : %d",*p);
}
